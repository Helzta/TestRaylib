using System;
using System.Collections.Generic;
using System.IO;
using Raylib_cs;

namespace TryRL
{
    class Program
    {
        static void Main(string[] args)
        {
            bool gameOn = true;

            while(gameOn)
            {
                platform.printHubMenu();
            }
        }
    }
}
